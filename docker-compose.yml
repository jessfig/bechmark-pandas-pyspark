services:
  duckdb:
    build:
      context: ./app
      dockerfile: docker/duckdb/DockerFile
    volumes:
      - ./app/data:/data

  spark:
    build:
      context: ./app
      dockerfile: docker/spark/DockerFile
    volumes:
      - ./app/data:/data

  # pandas:
  #   build: app/docker/pandas
  #   volumes:
  #     - ./data:/data
  #   depends_on:
  #     - duckdb